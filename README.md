# badges

This branch holds auto-generated shields.io "endpoint" badge JSON files.
It is updated automatically by CI (`.github/workflows/ci.yml`) on every
push to `main` and is not meant to be edited by hand.

Do not open pull requests against this branch.
